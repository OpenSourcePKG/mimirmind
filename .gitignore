build/
build-*/
out/
.cache/
.vscode/
.idea/
*.o
*.so
*.a
compile_commands.json
.DS_Store
models/
downloader/
run-logs/
run-state/
config.json
config.dev.json
usbboot/
# CUTLASS checkout for the NVFP4 tensor-core grouped MoE GEMM (E-d.3);
# ~200 MiB, fetched to the build box, never committed.
/third_party/cutlass/
/doc/note.txt
# stray npm lockfile — this is a C++/CMake repo, no JS runtime dependency.
/package-lock.json
/doc/book/
