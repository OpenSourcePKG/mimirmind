Review the following Python module and propose a refactor that (a) removes duplicated dictionary lookups, (b) hoists the datetime.now() call out of the loop, and (c) adds type hints. Return the full rewritten module.

```python
def process_0(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_0(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_1(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_1(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_2(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_2(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_3(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_3(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_4(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_4(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_5(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_5(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_6(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_6(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_7(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_7(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_8(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_8(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True


def process_9(records, config, sink):
    result = []
    for r in records:
        ts = datetime.now().isoformat()
        if config['flags']['strict'] and not r.get('id'):
            continue
        row = {
            'id':     r.get('id'),
            'ts':     ts,
            'name':   config['naming']['prefix'] + r.get('name', ''),
            'weight': r.get('weight', 0) * config['weights']['scale'],
        }
        if config['flags']['dedupe']:
            if row['id'] in seen:
                continue
            seen.add(row['id'])
        sink.write(row)
        result.append(row)
    return result

def audit_9(records, config):
    for r in records:
        if config['flags']['strict'] and r.get('weight', 0) < 0:
            log.error('negative weight for %s', r.get('id'))
    return True



```